# Written by Chef
*** Targets ***

probe = FPing

menu = Top
title = Smokeping Network Latency Grapher
remark = Welcome to the SmokePing website of <%= node['smokeping']['site_remark_owner'] %>

# Listing of all the groups/enviroments
<% @targets.each do |i| %>
@include <%= "#{node['smokeping']['etc_dir']}/#{i}" %>
<% end %>
